<%include ../partials/header.ejs%>


<div class="container">
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
        <br>
        <table class="table table-bordered table-striped table-hover">
            <!--- header row-->
            <tr>
                <th class="text-center">NAME</th>
                <th class="text-center">DESCRIPTION</th>
                                
            </tr>
            <!-- template row-->
            <% for(let count = 0; count < favList.length; count++){%>
<tr>
    <td class="text-center"><%= favList[count].name %></td>
    <td class="text-center"><%= favList[count].description %></td>
    </tr>
                <%}%>
            
        </table>
    </div>
    </div>
</div>
  <%include ../partials/footer.ejs%>